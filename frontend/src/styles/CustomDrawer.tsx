import { Box, ListItemText, styled } from "@mui/material";

export const CustomDrawerMainContainer = styled(Box)({
  position: "absolute",
  top: 0,
  right: 0,
});

export const DrawerListItem = styled(ListItemText)({
  textAlign: "right",
});

export const DrawerListBox = styled(Box)({
    width: "13.125rem"
})

